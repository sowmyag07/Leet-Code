class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        result = 0
        nums =[0] *n
        for i in range(len(nums)):
            nums[i]=start + 2 *i
            result = result ^ nums[i]
        return result